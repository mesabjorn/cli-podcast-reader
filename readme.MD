A CLI podcast crawler and opener in python

#Installation
1. Add rss feeds to feeds.txt in name;url format
2. Install python dependencies: pip install -r requirements.txt

# Usage
run `python main.py` to list your episodes, then select the id of the episode you want to play in your browser

# Optionally
Change MAX_AGE (default = 30 days) to obtain older episodes as well.